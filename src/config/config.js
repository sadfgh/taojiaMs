// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
  theme: {
    color: '#f0844a',
    // color: '#13c2c2',
    // mode: 'night',
    mode: 'light'
  },
  multiPage: true,
  animate: {
    name: 'roll',
    direction: 'default'
  }
}
